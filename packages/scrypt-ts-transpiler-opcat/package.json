{
  "name": "@opcat-labs/scrypt-ts-transpiler-opcat",
  "version": "2.0.2",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "description": "scrypt transpiler",
  "author": "OP_CAT Labs",
  "files": [
    "dist"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "tsc",
    "postbuild": "cd ../scrypt-ts-opcat && yarn gen:assets",
    "buildtest": "cd test && tspc -p tsconfig.json || true",
    "getBinary": "node ./dist/compile/getBinary.js",
    "pretest": "rimraf test/artifacts && yarn buildtest && yarn gen:spec",
    "test": "yarn mocha",
    "mocha": "cd test && mocha --parallel --jobs 1 --no-timeouts",
    "gen:spec": "npx -y tsx ./test/scripts/generateSpec.ts"
  },
  "dependencies": {
    "@opcat-labs/scrypt-ts-opcat": "2.0.2",
    "@phenomnomnominal/tsquery": "^6.1.2",
    "chalk": "^4.1.2",
    "get-proxy-settings": "^0.1.13",
    "https-proxy-agent": "^7.0.6",
    "json-bigint": "^1.0.0",
    "minimist": "^1.2.8",
    "mlly": "^1.7.4",
    "node-fetch": "^2.7.0",
    "reflect-metadata": "^0.1.13",
    "rimraf": "^6.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "ts-node": "^10.9.2",
    "ts-patch": "3.3.0"
  },
  "peerDependencies": {
    "typescript": "~5.3.3"
  }
}