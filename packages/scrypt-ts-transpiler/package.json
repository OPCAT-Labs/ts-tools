{
  "name": "@opcat-labs/scrypt-ts-transpiler",
  "version": "1.0.4",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "description": "scrypt transpiler",
  "files": [
    "dist"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "tsc",
    "postbuild": "cd ../scrypt-ts && yarn gen:assets",
    "buildtest": "cd test && tsc || true",
    "getBinary": "node ./dist/compile/getBinary.js",
    "pretest": "rimraf test/artifacts && yarn buildtest && yarn gen:spec",
    "test": "yarn mocha",
    "mocha": "cd test && mocha --parallel --jobs 5 --no-timeouts",
    "gen:spec": "npx -y tsx ./test/scripts/generateSpec.ts"
  },
  "dependencies": {
    "@opcat-labs/scrypt-ts": "1.0.4",
    "@phenomnomnominal/tsquery": "^6.1.2",
    "chalk": "^4.1.2",
    "get-proxy-settings": "^0.1.13",
    "https-proxy-agent": "^7.0.6",
    "json-bigint": "^1.0.0",
    "minimist": "^1.2.8",
    "mlly": "^1.7.4",
    "node-fetch": "^2.7.0",
    "reflect-metadata": "^0.1.13",
    "rimraf": "^6.0.1",
    "ts-patch": "=3.0.1",
    "typescript": "~5.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "ts-node": "^10.9.2"
  }
}
