import { ByteString } from '@opcat-labs/scrypt-ts';

export const MAX_TOKEN_OUTPUT = 5;

export const MAX_TOKEN_OUTPUT1 = 5n;

export const MAX_TOKEN_OUTPUT2 = 2 + 2;

export type int32 = bigint;
export const NEG = -1n;

export const MAX_TOKEN_OUTPUT3 = 1 + MAX_TOKEN_OUTPUT;

function adf() {}

type FFFF = bigint;

export interface FFF {
  aa: ByteString;
  bb: FFFF;
}

class AA {
  constructor() {}

  ASDF() {
    const BB = 3;
  }
}

const a = new AA();

const b = 'sdf';

let f;
