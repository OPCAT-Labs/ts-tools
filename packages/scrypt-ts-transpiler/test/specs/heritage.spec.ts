// import chai, { expect } from 'chai';
// import chaiExclude from 'chai-exclude';

// chai.use(chaiExclude);

//import { OrdinalCounter } from '../contracts/ordinalCounter';
// import { HeritageDemo1 } from '../contracts/heritageDemo1';
// import { HeritageDemo } from '../contracts/heritageDemo';
//import { HeritageCounter } from '../contracts/heritageCounter';
// import { loadTransformerResult } from '../utils/helper';
// describe('Test SmartContract with compile', () => {
// it('HeritageCounter should succeed when calling `compile` ', async () => {
//   const artifact = await HeritageCounter.compile();
//   expect(artifact).not.be.undefined;
// });
// it('heritageDemo should succeed when calling `compile` ', async () => {
//   const success = loadTransformerResult(HeritageDemo).success;
//   expect(success).to.be.true;
// });

// it('heritageDemo1 should succeed when calling `compile` ', async () => {
//   const success = loadTransformerResult(HeritageDemo1).success;
//   expect(success).to.be.true;
// });
// it('OrdinalCounter should succeed when calling `compile` ', async () => {
//   const artifact = await OrdinalCounter.compile();

//   //   expect(artifact).not.be.undefined;
//   // });
// });
